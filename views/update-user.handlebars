<h1>Update user: {{user.name}}</h1>
<form action="/users/update" class="form" method="POST">
    <input type="hidden" name="id" value="{{user.id}}">
    <div class="form-control">
        <label for="name">Name</label>
        <input type="text" name="name" placeholder="Write user name" value="{{user.name}}">
    </div>
    <div class="form-control">
        <label for="occupation">Occupation</label>
        <input type="text" name="occupation" placeholder="Write user occupation" value="{{user.occupation}}">
    </div>
    <div class="form-control">
        <label for="email">E-mail</label>
        <input type="text" name="email" placeholder="Write user email" value="{{user.email}}">
    </div>
    <br>
    <div class="form-control form-checkbox">
        {{#if user.newsletter}}
            <input type="checkbox" name="newsletter" checked>
        {{else}}
            <input type="checkbox" name="newsletter">
        {{/if}}
        <label for="newsletter">Subscribe user to newsletter?</label>
    </div>
    <div class="form-control form-checkbox">
        {{#if user.prime}}
            <input type="checkbox" name="prime" checked>
        {{else}}
            <input type="checkbox" name="prime">
        {{/if}}
        <label for="prime">Prime member</label>
    </div>
    <input class="submit-button" type="submit" value="Update">
    <br>
</form>
<br>
<div class="address-container">
    <h2>Addresses:</h2>
    <form action="/address/create" method="POST">
        {{!-- This is how we know the user data --}}
        <input type="hidden" name="UserId" value="{{user.id}}">
        <div class="form-control">
            <label for="country">Country</label>
            <input type="text" name="country" placeholder="Write user country">
        </div> 
        <div class="form-control">
            <label for="state">State</label>
            <input type="text" name="state" placeholder="Write user state">
        </div> 
        <div class="form-control">
            <label for="city">City</label>
            <input type="text" name="city" placeholder="Write user city">
        </div> 
        <div class="form-control">
            <label for="neighborhood">Neighborhood</label>
            <input type="text" name="neighborhood" placeholder="Write user neighborhood">
        </div> 
        <div class="form-control">
            <label for="street">Street</label>
            <input type="text" name="street" placeholder="Write user street">
        </div>        
        <div class="form-control">
            <label for="number">Number</label>
            <input type="text" name="number" placeholder="Write user number">
        </div>     
        <input type="submit" class="submit-button" value="Add address">                            
    </form>
</div>